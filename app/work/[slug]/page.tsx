import Link from "next/link";
import { notFound } from "next/navigation";
import { projects,imageUrl } from "@/data/projects";
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export default function CaseStudy({params}:{params:{slug:string}}){const p=projects.find(x=>x.slug===params.slug);if(!p)return notFound();return <main><div className="wrap"><nav className="nav"><Link className="brand" href="/">FAHIM<span>.</span></Link><div className="navlinks"><Link href="/work">Work</Link><Link href="/about">About</Link><Link href="/services">Services</Link><Link href="/archive">Archive</Link></div></nav><section className="case-hero"><div className="eyebrow mono">{p.number} / {p.category} / {p.year}</div><h1 className="case-title">{p.title}</h1><div className="case-meta"><span>{p.category}</span><span>{p.year}</span></div><p className="case-lead">{p.description}</p><div className="case-actions"><Link className="pill" href="/work">← All work</Link>{p.external&&<a className="pill" href={p.external} target="_blank" rel="noreferrer">View original ↗</a>}</div></section><section className="case-gallery">{p.images.map((img,i)=><img key={img} src={imageUrl(img)} alt={`${p.title} visual ${i+1}`}/>)}</section><section className="section"><div className="section-head"><h2>Next project</h2><Link className="mono" href="/work">Back to work ↗</Link></div></section></div></main>}
